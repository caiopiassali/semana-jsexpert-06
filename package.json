{
  "name": "semana-jsexpert-06",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "author": "Caio Piassali <caiopiassali@hotmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "node server/index.js",
    "live-reload": "nodemon server/index.js",
    "test": "set LOG_DISABLED=true && set NODE_OPTIONS=--experimental-vm-modules && jest --no-cache",
    "test:watch": "set LOG_DISABLED=true && set NODE_OPTIONS=--experimental-vm-modules && jest --no-cache --watchAll",
    "test:coverage": "set LOG_DISABLED=true && set NODE_OPTIONS=--experimental-vm-modules && jest --no-cache --coverage tests/unit"
  },
  "engines": {
    "node": "17"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "nodemon": "^2.0.15",
    "prettier": "^2.6.0"
  },
  "dependencies": {
    "pino": "7.8.0",
    "pino-pretty": "7.5.3"
  }
}
